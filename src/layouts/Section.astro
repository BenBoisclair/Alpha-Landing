---
import { cn } from "@/lib/utils";

export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn("px-[120px] pt-[60px] pb-[100px]", className)}>
    <slot />
</div>